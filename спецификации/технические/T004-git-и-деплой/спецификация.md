# T004 — Git и деплой

## Родительская власть

Главнее этой спецификации: `спецификации/бизнес-контекст.md`, `спецификации/правила-спецификаций.md` и связанные technical specs.

Эта спецификация описывает рабочий процесс. Она не может менять продукт или runtime-поведение.

## Связанные спецификации

- `спецификации/бизнес-контекст.md`
- `спецификации/правила-спецификаций.md`
- `спецификации/технические/T001-архитектура-приложения/спецификация.md`
- `спецификации/технические/T002-api-и-openai/спецификация.md`
- `спецификации/технические/T003-хранение-данных/спецификация.md`

## Текущая архитектура

Проект ведется как обычный Git-репозиторий. Коммиты должны быть короткими и понятными. Для документации используется тип `docs`.

Сборка выполняется через `npm run build`. Перед сборкой запускается `scripts/response-diversity-test.mjs`, затем `next build`.

## Implementation note — AGENTS workflow and git discipline

- Задача: сделать `AGENTS.md` главным operational rulebook для AI agents и будущей разработки.
- Связанные спецификации: `спецификации/правила-спецификаций.md`, `спецификации/процесс-реализации.md`, `спецификации/AGENTS.md`.
- Что меняем: добавляем строгие правила Git workflow, specification authority, safe changes и implementation lifecycle.
- Что не меняем: runtime-код, продуктовые требования, API, хранение данных и деплойные настройки.
- Как проверяем: `npm run lint`, `npm run build`.

## Важные файлы

- `package.json`
- `next.config.ts`
- `docs/specs/commit-rules.md`
- `scripts/response-diversity-test.mjs`
- `AGENTS.md`
- `спецификации/AGENTS.md`

## Поток данных

1. Разработчик возвращается на `main`.
2. Подтягивает последнюю версию `main`.
3. Проверяет чистое рабочее дерево.
4. Читает бизнес-контекст и связанные спецификации.
5. Создает одну сфокусированную ветку под задачу.
6. Вносит изменение.
7. Запускает `git status`.
8. Запускает `npm run lint`.
9. Запускает `npm run build`.
10. Делает один логический коммит.
11. Пушит только по явному запросу пользователя.
12. Не делает автоматический merge.

## Риски

- сборка может зависеть от тестов, которые проверяют поведение ответов
- изменение Next.js настроек может повлиять на runtime
- деплой на Vercel не должен полагаться на запись в локальный файл
- случайный коммит runtime-кода вместе с docs усложнит ревью

## Ограничения

- не коммитить лишние файлы
- не смешивать документацию и функциональные изменения без причины
- не менять `next.config.ts` без технической необходимости
- перед кодом читать документацию Next.js из `node_modules/next/dist/docs/`, если меняется Next.js поведение
- не пушить без явного запроса пользователя
- не мержить ветки автоматически
- не создавать параллельные структуры спецификаций или архитектуры

## Границы

Внутри: Git, проверка, сборка, деплойные ограничения и порядок коммитов.

Вне: продуктовые требования, API-контракт, схемы данных и интерфейс.

## Что эта спецификация не имеет права определять

- новые функции
- новые экраны
- модельный ответ
- правила хранения данных

## Что нельзя менять случайно

- npm scripts
- правила сборки
- правила спецификаций в `AGENTS.md`
- переменные окружения, связанные с памятью и OpenAI
