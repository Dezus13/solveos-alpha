# Spec Consistency Checklist

Before commit, check:

- Does User Flow match Decision Engine?
- Does Action System match User Flow?
- Does Identity Engine match Action outcomes?
- Does Pressure Layer match Action outcomes?
- Are all terms used consistently?
- Are Decision Journal, Identity Engine, and Pressure Layer named the same way?
- Is there any duplicated or conflicting logic?

If YES -> fix before commit
